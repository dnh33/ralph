---
name: cancel
description: "Cancel an active Ralph loop and clean up state"
allowed-tools:
  - Bash
---

Cancel the active Ralph loop by running:

```bash
bash "${CLAUDE_PLUGIN_ROOT}/scripts/ralph-cancel.sh"
```

Report the result to the user.
